export class Car {
  public make: string;
  public model: string;
  public price: number;

  constructor(make: string, model: string, price: number) {
    this.make = make;
    this.model = model;
    this.price = price;
  }

}
